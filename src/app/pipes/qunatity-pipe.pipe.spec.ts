import { QunatityPipePipe } from './qunatity-pipe.pipe';

describe('QunatityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QunatityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
